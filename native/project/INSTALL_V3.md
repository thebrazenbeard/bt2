# BT2 NATIVE PROJECT INSTALL V3

## Status
SOURCE INSTALLATION CANDIDATE. Presence in Git does not establish native Project installation, runtime binding, or behavioral effectiveness.

## Why V3 exists
The V2 Lantern filenames were consumed as conversation attachments before native Project installation, causing ChatGPT transport collision suffixes such as `(1)`. V3 moves the active native runtime to a fresh filename namespace.

## Project Instructions
Replace the active BT2 Project Instructions with exact `native/project/PROJECT_INSTRUCTIONS_V3.md` from the manifest-bound payload.

## Add-only Project files
Add exactly the four Lantern `_V3.md` files listed by `PROJECT_FILES_MANIFEST_V3.json`. Do not delete, overwrite, rename, or re-upload V1/V2 files.

Existing V1/V2 and transport-suffixed copies are historical/inactive whenever the corresponding canonical V3 file is present.

## Runtime
Target remains WoWSQL `bt2-479e4ad9`; currentness reads use `bt2_project_read` projection preflight -> B0 -> payload -> B1. No Supabase fallback. Producer mode remains `FROZEN_ZERO_PRODUCER`.
