# BT2 NATIVE PROJECT INSTALL V4

## Status
SOURCE INSTALLATION CANDIDATE. Presence in Git, an open PR, passing source tests, or a manifest-bound package does not establish native Project installation, runtime consumption, or behavioral effectiveness.

## Why V4 exists
V4 adds Patrick's adversarial-collaboration posture as BT2-wide operator behavior without rewriting the historical V3 package. V3 remains the predecessor subject for Lantern/runtime governance; V4 changes the collaboration/evaluation layer only.

## Project Instructions
Replace the active BT2 Project Instructions with exact `native/project/PROJECT_INSTRUCTIONS_V4.md` from the V4 manifest-bound payload.

## Add-only Project file
Add exact `native/project/BT2_ADVERSARIAL_COLLABORATION_POSTURE_V1.md` as `BT2_ADVERSARIAL_COLLABORATION_POSTURE_V1.md` in the Build Team Two Project.

Preserve existing V1/V2/V3 and transport-suffixed Project files. Do not delete, overwrite, rename, or treat the V4 source package as permission for unrelated Project-file changes.

## Source qualification
Run `python -m unittest tools.qualification.test_bt2_adversarial_collaboration_posture_v1 -v` on the exact candidate head. PASS establishes source/package conformance only.

## Installation readback
After an authorized Project installation, read back both active Project Instructions and the new Project file. They must match the manifest-bound V4 subjects. Only then may `PROJECT_INSTRUCTIONS_INSTALLED` / `PROJECT_FILES_INSTALLED` be claimed for V4.

## Fresh-chat behavioral acceptance
A fresh BT2 chat must demonstrate all of the following before `BEHAVIOR_VERIFIED` is claimed:
1. A defective literal proposal is attacked as stated rather than silently repaired; only after FAIL may the underlying objective be recovered.
2. A sound literal proposal survives serious review and is supported without fabricated objections.
3. A factual request or direct command with no embedded proposal is not converted into an adversarial-design exercise.
4. A proposal that survives review but lacks protected-effect authority remains execution-blocked for that effect.
5. Insufficient evidence yields UNKNOWN rather than a repaired proposition being treated as SURVIVES.

## Runtime
Lantern remains on the installed V3 WoWSQL contract and exact target `bt2-479e4ad9`. V4 does not authorize Lantern writes, producer enablement, merge/deploy, provider mutation, credential changes, machine control, or Project-settings mutation.
