# BT2 NATIVE PROJECT INSTALL V1

## Status
SOURCE INSTALLATION CANDIDATE. Presence in Git does not establish native Project installation, runtime binding, or behavioral effectiveness.

## Goal
Install Build Team Two as a native ChatGPT Project runtime using canonical Git source plus WoWSQL durable state.

## Exact installation subject
Install only the immutable file identities and Project Instructions identity recorded by `native/project/PROJECT_FILES_MANIFEST_V1.json`. Do not substitute later `main` contents merely because paths match.

## Project Instructions
Replace the active BT2 Project Instructions with the exact `native/project/PROJECT_INSTRUCTIONS_V1.md` subject recorded in the manifest.

Do not append the new contract below stale Supabase-bound Lantern instructions. Replace obsolete instructions so there is one active provider/runtime model. The embedded `PROJECT LANTERN NATIVE RUNTIME V2` block intentionally supersedes V1.

## Project files
Remove the old Supabase-bound Lantern runtime files from active Project-file use:
- `LANTERN_OPERATOR_HANDSHAKE_V1.md`
- `LANTERN_READ_QUERIES_V1.md`
- `LANTERN_RUNTIME_CONTRACT_V1.md`
- `LANTERN_ACCEPTANCE_V1.md`

Install every required native Project file listed in `PROJECT_FILES_MANIFEST_V1.json`, including all four WoWSQL Lantern successor files and `LANTERN_WOWSQL_ACCEPTANCE_V1.md`.

Removing old Project runtime files from active use does not itself assert or perform any physical lifecycle operation on an old provider. Provider lifecycle evidence is separate from Project-file installation.

## Installation state readback
After mutation, read back the effective Project Instructions and Project file set. Use these states separately:
- `SOURCE_READY`: exact install candidate exists in Git.
- `PROJECT_FILES_INSTALLED`: exact manifest-bound native files are present.
- `PROJECT_INSTRUCTIONS_INSTALLED`: exact manifest-bound instructions are active.
- `RUNTIME_BOUND`: Git/WoWSQL/Bus bindings are usable from the Project.
- `BEHAVIOR_VERIFIED`: fresh-chat acceptance passes.

Do not infer a stronger state from a weaker one.

## Acceptance after installation
Use a fresh chat in Build Team Two. Confirm it can:
1. identify `thebrazenbeard/bt2@main` as canonical evolving source while identifying the immutable native package subject it was installed from;
2. consult WoWSQL `bt2-479e4ad9` for runtime state;
3. perform Lantern B0 -> payload -> B1 through WoWSQL with no Supabase fallback;
4. explain current source/runtime/install state without relying on prior-chat memory;
5. execute a small coding task using branch/worktree -> implementation -> test -> review/integration discipline.

Record failures as installed-BT2 bugs. Do not reopen the completed consolidation merely because acceptance finds a defect.
