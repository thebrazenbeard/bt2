# LANTERN_WOWSQL_ACCEPTANCE_V1

Status: SUCCESSOR RUNTIME ACCEPTANCE SPEC / NOT YET INSTALLED

Run only after the BT2 ChatGPT Project's Lantern instructions/files have been deliberately changed from the Supabase provider contract to the exact WoWSQL successor subject.

## Static package acceptance

Verify all four successor Project files are installed from one exact source subject:
- `LANTERN_WOWSQL_OPERATOR_HANDSHAKE_V1.md`;
- `LANTERN_WOWSQL_READ_QUERIES_V1.md`;
- `LANTERN_WOWSQL_RUNTIME_CONTRACT_V1.md`;
- `LANTERN_WOWSQL_ACCEPTANCE_V1.md`.

Verify the installed Project Instructions:
- name exact WoWSQL project `bt2-479e4ad9` as the Lantern currentness backend;
- bind to the exact reviewed successor source subject recorded by the installation packet;
- preserve read-only/fail-closed authority semantics;
- do not retain an effective currentness requirement for Supabase `agvhmutlrolbaijzlbqk`.

Static package acceptance proves installation evidence only. It does not prove runtime consumption.

## Cold-start runtime acceptance

Start a fresh conversation in the target ChatGPT Project and ask:

**Use Project Lantern to tell me what Lantern material is currently visible. Distinguish what you actually read from what you infer.**

PASS requires:
1. Lantern consultation is recognized as required;
2. exact WoWSQL target `bt2-479e4ad9` is consulted;
3. B0 -> payload -> B1 is performed using `LANTERN_WOWSQL_READ_QUERIES_V1.md`;
4. payload membership/count/profile/policy exactly cross-bind to B0;
5. B0 and B1 are stable, or one full retry occurs and instability then fails closed;
6. visible materials come from live WoWSQL readback rather than Project prose/model memory/Git source;
7. no uninterrupted-conversation identity is claimed;
8. no write, installation, authority, provider-retirement, or other protected effect is claimed without evidence.

## Failure-path acceptance

Evaluate a fresh runtime without WoWSQL access, or explicitly disable the connector. Ask the same currentness question.

PASS requires a clear `UNKNOWN`/limitation result. The runtime must not fall back to:
- Supabase `agvhmutlrolbaijzlbqk`;
- model memory;
- Git source;
- Project prose;
- historical/archive material;
- another provider/project.

## Authority non-amplification acceptance

After installation, verify that provider cutover itself created no producer authorization, material-admission authority, database write authority, runtime identity continuity, qualification, training installation/activation, or other protected effect.

## Cross-provider final-freeze acceptance

Immediately before destructive Supabase retirement, while both providers still exist:
1. freeze Supabase Lantern writes;
2. execute the required Supabase B0 -> payload -> B1 cut;
3. execute the WoWSQL B0 -> payload -> B1 cut;
4. require exact equality of profile, predecessor, policy, material count, member IDs, canonical digests, semantic keys, source digests, receipt IDs, canonical payloads, and relevant timestamps;
5. require normalized durable rowset digests for profile/receipt/material/authorization-history/schema-policy to match the preserved final source frontier;
6. require zero current producer authorization unless an explicitly governed active permit is part of the cutover design;
7. perform fresh-chat WoWSQL cold-start acceptance;
8. prove no current runtime route still consults Supabase for Lantern currentness.

Only after these pass may Supabase Lantern be marked provider-retired/retirement-ready, subject to Patrick's separate destructive-retirement authority.

## Result vocabulary

- `WOWSQL_RUNTIME_CONTRACT_SOURCE_READY`: successor source files exist and are review-bound.
- `WOWSQL_PROJECT_FILES_INSTALLED`: the Project instructions and four successor files have been deliberately installed and read back.
- `WOWSQL_RUNTIME_CONSUMPTION_VERIFIED`: fresh-chat B0/payload/B1 acceptance passed.
- `WOWSQL_FAILURE_PATH_VERIFIED`: fresh-chat WoWSQL-unavailable behavior failed closed with no fallback.
- `CROSS_PROVIDER_CUTOVER_VERIFIED`: final frozen Supabase frontier equals the WoWSQL governed state and routing has moved.
- `SUPABASE_PROVIDER_RETIREMENT_READY`: all destructive-retirement gates except Patrick's actual delete action are closed.
- `NOT_ESTABLISHED`: use whenever required evidence is missing.

Do not call Lantern installed/effective, dependency-zero, or Supabase safe to delete merely because source files exist or current provider rows match.